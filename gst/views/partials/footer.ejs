<footer class="bg-light text-center text-lg-start shadow-sm mt-4" style="position: fixed; bottom: 0; left: 0; width: 100%; z-index: 1000;">
    <div class="container p-3">
      <div class="row align-items-center">
        
        <div class="col-md-6 text-md-start">
          <h6 class="fw-bold mb-1">GST Asset Management System</h6>
          <p class="mb-0 text-muted small">Version 1.0.0 | Developed by V11</p>
        </div>
  
        <div class="col-md-6 text-md-end mt-3 mt-md-0">
          <p class="mb-0 text-muted small" id="datetime">Loading date and time...</p>
        </div>
  
      </div>
    </div>
  </footer>
  
  <script>
  // Dynamically set current date and time
  function updateDateTime() {
      const now = new Date();
      const formatted = now.toLocaleString('en-IN', { hour12: true });
      document.getElementById('datetime').textContent = `Current Time: ${formatted}`;
  }
  
  // Update time every second
  setInterval(updateDateTime, 1000);
  updateDateTime();
  </script>
  
</body>
</html>